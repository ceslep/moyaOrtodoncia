{
  "name": "moyaortodoncia",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && git -C dist init && git -C dist remote add origin https://github.com/ceslep/moyaOrtodoncia.git 2>/dev/null; git -C dist checkout -b gh-pages && git -C dist add -A && git -C dist commit -m \"deploy\" && git -C dist push -u origin gh-pages --force && rm -rf dist/.git",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.13.2",
    "gh-pages": "^6.3.0",
    "svelte": "^5.56.4",
    "svelte-check": "^4.7.1",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "thinking-orbs": "^0.1.1"
  }
}
